<Window x:Class="RefaccionariaPOS.Views.InventarioView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inventario de Refacciones" Height="620" Width="1100"
        WindowStartupLocation="CenterScreen" Background="#F4F6F9">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Catálogo de Productos" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <Button Content="+ Nuevo Producto" Width="150" Height="35" Background="#27AE60" Foreground="White" FontWeight="Bold" Cursor="Hand" HorizontalAlignment="Right" Click="BtnNuevo_Click"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="14" Margin="0,0,0,15" BorderBrush="#BFDBFE" BorderThickness="1">
            <WrapPanel VerticalAlignment="Center">
                <StackPanel Margin="0,0,14,0">
                    <TextBlock Text="Buscar Producto" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                    <TextBox x:Name="txtBuscar" Width="260" TextChanged="TxtBuscar_TextChanged"/>
                </StackPanel>

                <StackPanel Margin="0,0,14,0">
                    <TextBlock Text="Tipo de Pieza" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                    <ComboBox x:Name="cmbCategoria" Width="180" SelectionChanged="CmbCategoria_SelectionChanged"/>
                </StackPanel>

                <CheckBox x:Name="chkBajoStock"
                          Content="Solo bajo stock"
                          VerticalAlignment="Bottom"
                          Margin="0,0,14,8"
                          FontWeight="SemiBold"
                          Click="ChkBajoStock_Click"/>

                <Button Content="Limpiar filtros"
                        Width="120"
                        Height="34"
                        Margin="0,18,0,0"
                        Background="#64748B"
                        Click="BtnLimpiarFiltros_Click"/>
            </WrapPanel>
        </Border>

        <DataGrid x:Name="dgInventario" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" Background="White" RowHeight="30" AlternatingRowBackground="#ecf0f1">

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Actualizar Stock Físico" Click="MenuActualizarStock_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding CodigoBarras}" Width="100"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="200"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Categoria}" Width="130"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*"/>
                <DataGridTextColumn Header="Precio Venta" Binding="{Binding PrecioVenta, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80"/>
                <DataGridTextColumn Header="Mínimo" Binding="{Binding StockMinimo}" Width="80"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding EstadoStock}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
